rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can read and write tricks (this app has no login by design).
    match /tricks/{trickId} {
      allow read, write: if true;
    }
    match /_meta/{docId} {
      allow read, write: if true;
    }
  }
}